[project]
name = "dl-project"
version = "0.1.0"
description = "Deep learning project for AI417"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
requires-python = ">=3.14,<3.15"
dependencies = [
    "torch (>=2.10.0,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
